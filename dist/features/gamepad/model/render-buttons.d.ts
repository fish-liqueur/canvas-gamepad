import Button from "@entities/button";
import { ButtonStyleKit } from "@entities/button";
export default function renderButtons(options: ButtonStyleKit, context: CanvasRenderingContext2D, canvasSide: number, activeButton?: Button | null): Button[];
